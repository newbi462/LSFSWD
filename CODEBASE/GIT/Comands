//Make Branch
git checkout -b 'branch-name'

// Publish/PUSH branch
git push -u origin branch-name


git pull
git fetch --all



To list remote branches:
git branch -r

You can check them out as local branches with:
git checkout -b LocalName origin/remotebranchname


If you want to list all remote branches:
git branch -a


After check out to avoid publish need and link local to remote
git branch --set-upstream-to=origin/feature/role-schema feature/role-schema


git checkout -b feature/role-schema
